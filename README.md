# Fidelity Trading Platform (Backend + DB Integration)

Backend API and database system for authentication, dashboard data, stock reward & review engine, spin-wheel logic, admin controls, and notifications.

---

## Run

```bash
npm start

